# Figures de prédiction

Ce dossier contient les figures générées par les notebooks et scripts du dossier `prediction/`.

Les sorties tabulaires associées restent dans `data/processed/prediction/` afin de garder les données séparées des figures.
